#include "main.h"

/**
 * _isdigit(int c)- function to check for character
 * @c: parameter to check
 *
 * Description: this functions checks if
 * entered arg is a digit
 * Return: returns 1 if success
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
